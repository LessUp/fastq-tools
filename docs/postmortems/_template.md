<!--
postmortem 模板：复制本文件为 YYYY-MM-DD-<slug>.md 后填充。
下划线前缀使其在索引中靠后；不要直接编辑本文件作为条目。
-->

# <一句话标题>

| 字段     | 值 |
|----------|----|
| 日期     | YYYY-MM-DD |
| 严重度   | S0 \| S1 \| S2 \| S3 |
| 影响范围 | <受影响模块/命令/用户场景> |
| 状态     | open \| fixed \| monitoring \| wontfix |
| 相关 commit | `<sha>` <可选多行> |
| 相关 issue  | #<编号> <可选> |

## 现象

客观描述可观察到的错误行为：报错信息、错误输出、性能异常、数据错误等。不掺入猜测。

## 复现步骤

可重复的最小步骤。包含：构建配置、输入数据规模/来源、调用命令、环境（编译器/库版本）。

```bash
# 复现命令
```

## 定位过程

按时间顺序记录关键假设与验证。失败的假设也保留，标注"假设→证伪"。

1. **假设 A**：<描述>
   - 验证手段：<日志/grep/profiler/最小用例>
   - 结论：<证实 / 证伪 / 部分成立>
2. **假设 B**：…

> 引用关键日志/堆栈/数据片段时用代码块；长内容可外链到 commit 或 gist。

## 根因

最终确认的根本原因。一句话概括 + 必要的机制说明。区分"触发条件"与"潜在缺陷"。

## 修复方案

- 改动要点（不贴 diff，链接 commit）。
- 是否引入新测试用例（链接 `tests/` 路径）。
- 是否需要迁移/兼容性处理。

## 验证

- 复现命令在修复后行为如何。
- 新增/相关的测试结果：`./scripts/core/test --filter '<pattern>'`。
- 回归检查范围。

## 教训与后续防范

- 该问题暴露的流程/约定缺口。
- 防范措施：测试用例、CI 门槛、代码规范、文档补充等。
- 是否需要更新 `AGENTS.md` 或相关模块 README。

## 状态变更记录

- YYYY-MM-DD：open → fixed（commit `<sha>`）
- YYYY-MM-DD：fixed → monitoring（观测期开始）
