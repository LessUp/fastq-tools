# DevContainer 简化配置使用说明补充（2026-01-17）

## 背景

需要说明如何使用 `devcontainer.simple.json` 进行构建，并明确配置选择方式。

## 变更

- `.devcontainer/README.md`
  - 补充 `devcontainer.simple.json` 的 VS Code 选择步骤与 devcontainer CLI 用法。

## 影响

- 便于开发者在不使用 docker-compose 的场景下快速启动 DevContainer。

## 回退方案

- 移除 `README.md` 中新增的简化配置使用说明。
