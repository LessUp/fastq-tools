# 修复 stat 命令必需参数校验（2026-01-15）

## 背景
- `stat` 子命令未校验 `--input`/`--output` 必需参数，解析后直接访问会导致异常或空路径。

## 本次变更
- 编辑 `src/cli/commands/stat_command.cpp`
  - 在解析后补充必需参数检查，缺失时输出错误提示与帮助信息并返回非 0。

## 影响范围
- `FastQTools stat` 在缺失参数时行为更明确、不会继续执行统计。

## 兼容性
- 正常调用不受影响。

## 回退方案
- 如需回退，可移除 `stat_command.cpp` 中的必需参数检查逻辑。
